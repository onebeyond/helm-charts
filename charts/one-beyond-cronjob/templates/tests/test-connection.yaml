apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "one-beyond-cronjob.fullname" . }}-test-connection"
  labels:
    {{- include "one-beyond-cronjob.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "one-beyond-cronjob.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
