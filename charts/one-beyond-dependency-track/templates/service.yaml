apiVersion: v1
kind: Service
metadata:
  name: {{ include "one-beyond-dependency-track.fullname" . }}
  labels:
    {{- include "one-beyond-dependency-track.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "one-beyond-dependency-track.selectorLabels" . | nindent 4 }}
